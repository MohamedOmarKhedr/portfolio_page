import 'package:flutter/material.dart';

class HeroConstants {
  static const String name = 'Mohamed Omar Khedr';
  static const String title = 'Flutter Developer';
  static const String description = "I build mobile apps that are easy to use, fast, and dependable. "
      "I make sure they start with a strong base so they can grow and handle more features in the future.";
  
  static const double circleRadius = 125.0;
  static const double circleOffset = 100.0;
  static const double nameSpacing = 20.0;
  static const double titleSpacing = 35.0;
  static const double descriptionSpacing = 50.0;
  
  static const Map<String, Color> buttonColors = {
    'cv': Color(0xFF6366F1),
    'github': Color(0xFF10B981),
  };
}